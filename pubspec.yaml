name: flutter_zebra_scanner_palette
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.6 <4.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  device_info_plus: ^9.1.0
  flutter:
    sdk: flutter
  flutter_datawedge: ^2.1.0
  flutter_dotenv: ^5.1.0
  get: ^4.6.6
  http: ^1.1.0
  internet_connection_checker: ^1.0.0+1
  location: ^5.0.3

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - lib/mock/user.json
    - lib/.env
